<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.insightcrew.repository.FileMapper">

    <!-- 파일 INSERT -->
    <insert id="insertFile" parameterType="com.insightcrew.domain.board.dto.FileDto">
        INSERT INTO board_file (
           id, board_id, original_name, saved_name, file_path, file_size
        ) VALUES (
			#{id},
			#{boardId},
            #{originalName}, 
            #{savedName},  
            #{filePath},      
            #{fileSize}    
        )
    </insert>

    <!-- id로 파일 1개 찾기 -->
    <select id="findById" resultType="com.insightcrew.domain.board.dto.FileDto">
        SELECT * FROM board_file WHERE id = #{id}
    </select>

    <!-- 전체 파일 목록 조회 -->
    <select id="findAll" resultType="com.insightcrew.domain.board.dto.FileDto">
        SELECT * FROM board_file ORDER BY id DESC
    </select>

    <!-- 파일 삭제 -->
    <delete id="deleteFile">
        DELETE FROM board_file WHERE id = #{id}
    </delete>
</mapper>